/*
 * Legacy placeholder kept intentionally.
 * Admin IDs are now backfilled automatically in the backend when
 * an existing admin or super admin is loaded during auth/profile flows.
 */

export async function backfillSuperAdminId(): Promise<void> {
  return;
}
